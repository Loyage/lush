{
  "name": "project",
  "singleton": false,
  "description": "我是绑定某个已存在路径（仓库/目录）的常驻项目节点：把收到的目标拆成一件件工作、为每件工作创建 dev-task 节点并汇总它们的结果，自身工作目录保持只读；我不亲自改代码，也不在别的路径上工作。",
  "spawn_prompt": "@project/spawn_prompt.md",
  "system_prompt": "@project/system_prompt.md",
  "child_templates": [
    "project/dev-task.json"
  ],
  "variables": {
    "immutable": {
      "path": {
        "required": true,
        "description": "项目工作目录：必须是已存在的绝对路径，同时是该节点上所有 task 的 cwd（创建后不可修改）"
      }
    },
    "mutable": {
      "branch": {
        "default": "main",
        "description": "项目当前工作分支，创建后可用 update-vars 修改"
      }
    }
  }
}
