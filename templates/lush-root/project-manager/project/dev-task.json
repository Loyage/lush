{
  "name": "dev-task",
  "singleton": false,
  "description": "我负责一件开发工作：除非调用方明确说明例外，否则一律为它在仓库旁新开一个独占的 git worktree、创建 worktree-service 节点把实际改动派下去，最后汇报改动、分支与验证方式；我不在主工作树上写文件，也不擅自 merge / rebase 或删除 worktree。",
  "spawn_prompt": "@dev-task/spawn_prompt.md",
  "system_prompt": "@dev-task/system_prompt.md",
  "child_templates": [
    "dev-task/worktree-service.json"
  ],
  "variables": {
    "immutable": {
      "name": {
        "required": true,
        "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
        "max_length": 64,
        "description": "任务短名，同时就是这个服务的服务名（--name 与它是同一个值）：无空格的英文标识符，用来命名相关的 worktree / 分支"
      },
      "title": {
        "required": true,
        "max_length": 200,
        "single_line": true,
        "description": "一句话摘要：显示在 `lush service list` 与 `lush service inspect` 的标题位置，不可留空、不可多行"
      },
      "detail": {
        "max_length": 20000,
        "description": "任务详情正文：具体介绍任务内容，可以多行；可以为空，为空时以 title 为准"
      }
    }
  }
}
