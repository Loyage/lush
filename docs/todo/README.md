# 改进候选清单

这里汇总对 Lush 的并行代码审查，供维护者决定下一步投入。内容是**待评审建议，不是已批准需求、完成记录或安全认证**；当前实现仍以[工程文档](../engineering/README.md)和[接口参考](../reference/README.md)为准。

## 从哪里开始

先读[优先级与实施前决策](00-priorities.md)，再按兴趣进入专题；具体依据、触发条件、反例、方案取舍和验收标准只在专题中维护。

| 专题 | 条目 | 回答的问题 |
|---|---|---|
| [运行时与持久化](01-runtime-persistence.md) | R-01～R-07 | 清空、锁、崩溃恢复、调度容量、消息与历史规模是否可靠？ |
| [Git / Worktree 与交付](02-git-worktrees.md) | G-01～G-06 | 活动工作区是否会被删、验收是否对应固定代码、回收与图读取能否改进？ |
| [安全与边界](03-security-boundaries.md) | S-01～S-06 | 多项目请求、来源校验、IPC、限流与传输预算是否守住边界？ |
| [Web / 桌面体验](04-web-desktop-experience.md) | U-01～U-07 | 输入是否可靠提交、失败是否丢字、异步页面与桌面偏好是否稳定？ |
| [测试与工程维护](05-testing-maintainability.md) | E-01～E-06 | 测试隔离、命令契约、兼容矩阵、真实 UI 与性能证据哪里可补强？ |

共 **32 条**：P1 7 条、P2 24 条、P3 1 条。P1 中 R-03、G-03 属于已披露限制或既定使用前提之外的加固机会，不能合称“7 个新漏洞”。优先级不是漏洞评分，也不是排期承诺。

## 当前状态复核提示

本清单按 2026-09-23 的旧基线审查，不能直接代表当前 HEAD。此后已切换到 say / Task 中心输入与交付流程；特别是 U-01 原先针对“当前输入加入草稿后批量提交”的 UI 路径，当前主流程已变，已在专题标为可能过时，需按现行交互复核。各条报告中的源码行号、接口名称和测试数量均是当时证据，不是当前验证结果。尚未逐项复测的条目保持候选，不据此断言已修复。

2026-09-26 第一批修复：R-01（清空写入门与在途写入收尾，`1d84d59`）、R-02（内核 flock 取代 PID 回收竞态，`f36cd45`）、G-02（Candidate 验收核验固定树，`0dc36ef`）、G-01（归档纳入无 branch 字段的实际使用者，`a8c1699`）已实现并附回归；S-01 经用户确认本轮暂不实施，身份模型仍待定。其余条目仍为候选。

## 如何读证据

- **已复现**：在临时 fixture、mock、DOM stub 或受控子进程中观察到所述结果；括号中的层级是证据边界，不自动外推至真实浏览器或生产环境。
- **代码确认**：能追踪具体代码路径，但未必完成端到端复现；“缺少测试”不等于功能已坏。
- **待验证**：需要真实客户端、特定平台或规模量测后才能下结论。
- **S / M / L**：相对改动量估计，不是工时；架构或产品选择另行确认。各条目默认状态均为“待评审”。

## 审查基线与隔离方式

- 审查日期：2026-09-23；统一基线：`99fcbc993640c057488532a19ca08814ab60b73e`。
- 用 Orca 启动 **5 个并行 agent、5 个独立顶层 worktree**，从当时与 main 相同的 origin/main 创建；不是在一个目录里模拟分工。
- Worktree 位于 `~/orca/workspaces/lush/`：`audit-runtime`、`audit-worktrees`、`audit-security`、`audit-web`、`audit-engineering`；分支分别为 `Loyage/<同名>`。
- 协调记录：Run `run_ca4441285065`，五个 Task 均成功结算；worker 终端已释放，worktree 与原始报告保留，未强制删除或自动合并。
- 只将报告复制回当前项目并复核，补充索引及阅读顺序；没有修改业务代码、提交或推送，也没有启停用户 daemon、合并用户分支或修改既有任务。
- 复现仅使用临时项目、mock 与可控进程。没有真实模型调用、浏览器攻击、真实数据破坏试验；测试用进程与临时目录均已清理。

## 验证摘要

- 工程 worker 在隔离 HOME / XDG / Git 配置环境运行基线 `bun run test`：**588 pass / 0 fail，120 个测试文件，4771 个断言**；不能用上一版本的测试数量代替本次结果。
- 各专题另有定向回归和边界探针，完整命令与证据局限见各报告；这些测试与全量测试有重叠，数量不叠加。
- 基线 `bun run measure:read-performance` 的现有预算全部通过；这不覆盖 G-06 的完整图读取或真实浏览器渲染成本。
- 协调者重新阅读关键调用链，在隔离 HOME / XDG / Git 配置环境执行 `bun run /tmp/lush-todo-review.cHuhIZ/verify.mjs`，独立复现 **R-01（清空丢新增草稿）、G-01（归档删除活动 planner anchor）、S-01（旧页面操作路由到 B，mock HTTP）**；3 项验证全部通过，未对真实项目执行这些动作，临时脚本已清理。
- 汇总后的 `bun run docs:check`：**59 篇 Markdown 通过**；`bun run test test/docs-check.test.js test/web/docs.test.js`：**8 pass / 0 fail**；`git diff --check` 及新增文档格式/条目 ID 检查通过。源码位置均相对于审查基线，后续实现变化时需重新核对。
- 未覆盖真实浏览器 / Electron / 读屏器、最低 Bun 版本与所有部署组合；无法据此声称穷尽全部问题。

## 后续维护

选中条目后先确认产品/架构选项，再把最小复现转成持久回归测试，最后实施。完成时保留稳定 ID、更新状态并附提交与验证记录，不把建议原地改写成早已存在的能力。相互关联的条目和安全并行边界见[优先级与实施前决策](00-priorities.md)。

[返回文档地图](../README.md)
